tags:
  - 'Face Recognition ML Model'
summary: 'Retrain face model.'
description: 'The model should be retrained if updates to the face database were done with the `retrain` flag set to `false`.'
operationId: retrainModel
produces:
  - ""
security:
  - ApiKeyAuth: []
responses:
  '204':
    description: 'Model for the api key is forced to stop retraining'
  '400':
    $ref: '#/components/responses/BadRequestResponse'
  '500':
    $ref: '#/components/responses/InternalServerErrorResponse'
