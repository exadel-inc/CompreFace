# TODO EFRS-103 Create unit test that checks DTO -> JSON conversion
# Hint: scan_faces result must be mocked (manually create some ScannedFace objects and check that the user gets the same values as in the object)
