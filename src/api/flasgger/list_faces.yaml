tags:
  - "Recognition"
summary: "Retrieve all face names for specified API Key"
description: "This REST endpoint could be used to retrieve all the face names for the specified API Key"
operationId: "getImages"
produces:
  - "application/json"
parameters:
  - name: "X-Api-Key"
    in: "header"
    description: "Your API Key"
    required: true
    type: "string"
responses:
  200:
    description: "successful operation"
    schema:
      $ref: "#/definitions/GetAllNamesResponse"
  400:
    description: "Api key not specified"