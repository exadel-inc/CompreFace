server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5442/frs
    username: postgres
    password: postgres
  jpa:
    properties:
      hibernate:
        jdbc.lob.non_contextual_creation: true # fix for Caused by: java.sql.SQLFeatureNotSupportedException: Method org.postgresql.jdbc.PgConnection.createClob() is not yet implemented.
        format_sql: false
    hibernate:
      ddl-auto: none
    show_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: true
    generate-ddl: false

security:
  oauth2:
    client:
      client-id: frs-service
      client-secret: H)g4N3&DgZV59m@Y
  jwt:
    client-id: frs-service
    client-secret: "{noop}H)g4N3&DgZV59m@Y"
    grant-type: password
    scope-read: read
    scope-write: write
    resource-ids: testjwtresourceid
  signing-key: MaYzkSjmkzPC57L
  encoding-strength: 256
  security-realm: Spring Boot JWT Example Realm

swagger:
  auth:
    server: http://localhost:8080
  info:
    title: FRS