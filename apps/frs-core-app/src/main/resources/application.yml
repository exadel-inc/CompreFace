server:
  port: 8081

spring:
  profiles:
    active: dev

---

spring:
  jackson:
    serialization:
      indent-output: true
  data:
    mongodb:
      uri: ${MONGO_URL:mongodb://msqv500.exadel.by:27017}
      database: efrs_db

---

spring:
  profiles: dev
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: false

