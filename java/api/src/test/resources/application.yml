spring:
  cache:
    type: none
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL10Dialect

environment:
  servers:
    PYTHON:
      url: ${PYTHON_URL:http://compreface-core:3000}