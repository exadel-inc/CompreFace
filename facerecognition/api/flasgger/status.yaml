tags:
  - "Recognition"
summary: "Get server health status"
description: "Endpoint useful for checking whether the server is up and running"
operationId: "status"
produces:
  - "application/json"
responses:
  200:
    description: "Server is live"
    schema:
      type: "object"
      properties:
        status:
          type: "string"
      example: "status: OK"